{
  "name": "pipelines4s",
  "description": "Tasks and utilities for compilation, testing, and deployment of Scala and/or Java based projects.",
  "repository": {
    "type": "git",
    "url": "https://github.com/duchessa/pipelines4s"
  },
  "scripts": {
    "clean": "node clean.js",
    "build:bundle": "webpack --config webpack.config.js --env production",
    "build": "npm install && npm run build:bundle && node_modules/.bin/tfx extension create --manifest dist/vss-extension.json --root dist"
  },
  "devDependencies": {
    "@types/node": "^14.14.43",
    "@types/q": "^1.5.4",
    "@types/semver": "^5.5.0",
    "copy-webpack-plugin": "^8.1.1",
    "tfx-cli": "^0.9.2",
    "ts-loader": "^9.1.2",
    "typescript": "^4.2.4",
    "webpack": "^5.36.0",
    "webpack-cli": "^4.6.0"
  },
  "dependencies": {
    "azure-pipelines-task-lib": "^3.1.2",
    "azure-pipelines-tool-lib": "^1.0.2",
    "semver": "^7.3.5"
  }
}
