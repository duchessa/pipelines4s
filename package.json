{
  "name": "pipelines4s",
  "description": "Tasks and utilities for compilation, testing, and deployment of Scala and/or Java based projects.",
  "repository": {
    "type": "git",
    "url": "https://github.com/duchessa/pipelines4s"
  },
  "scripts": {
    "clean": "node clean.js",
    "build-tasks": "npm --prefix=tasks/setup-sbt run build && npm --prefix=tasks/setup-graalvm run build",
    "build": "npm run build-tasks && npm install && node_modules/.bin/tfx extension create --manifest vss-extension.json"
  },
  "devDependencies": {
    "tfx-cli": "^0.9.0"
  }
}
